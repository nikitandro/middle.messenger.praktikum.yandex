// language=handlebars
export default /*hbs*/ `
    <div class="input-container">
        <input type="{{type}}" name="{{name}}" class="input" data-label="{{label}}" placeholder="{{label}}">
    </div>`;
